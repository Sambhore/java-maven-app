version: 0.0
os: linux

files:
  - source: target/ROOT.war
    destination: /opt/tomcat/webapps/

hooks:
  AfterInstall:
    - location: scripts/restart_tomcat.sh
      timeout: 300
      runas: root

